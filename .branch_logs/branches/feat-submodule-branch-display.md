# feat/submodule-branch-display

<!-- git-guard: ref=refs/heads/feat/submodule-branch-display -->

- Fix catalog submodule display semantics so repo branch labels show the
  submodule repo branch while the superproject branch remains contextual
  metadata.
- Include Git Guard runtime auto-sync updates required by the installed hook.
