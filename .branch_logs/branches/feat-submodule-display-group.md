# feat/submodule-display-group

- Add submodule display metadata so sibling submodule instances group by parent repo and submodule path.
- Show the superproject branch as the primary branch label for submodule instances while keeping child branch/commit as secondary metadata.
- Update catalog enrichment to let host agent clear stale worktree status messages on services.
